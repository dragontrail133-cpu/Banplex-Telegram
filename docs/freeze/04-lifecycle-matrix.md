# Lifecycle Matrix

Freeze date: `2026-04-19`
Runtime reconciliation: `2026-04-23`

## Global lifecycle rules

- Status `unpaid / partial / paid` dimiliki settlement-aware parent atau child, tetapi tidak mengubah identitas parent domain.
- Record yang sudah punya payment history tidak boleh dihapus langsung dari active flow.
- Cancellation payment adalah `void/soft-delete-log`, bukan hard delete.
- Local draft hanya diizinkan untuk `Faktur Barang` dan `Surat Jalan Barang`.
- `Halaman Absensi` existing adalah surface input absensi harian; `Catatan Absensi` adalah halaman histori/filter/rekap yang terpisah.
- Dalam konteks payroll operasional, worker adalah parent utama grouping; payroll payable yang boleh muncul di `Jurnal` dan `Riwayat` adalah `Tagihan Upah` per worker.
- `Riwayat` adalah completed/history surface; restore dan deleted recovery memakai `Recycle Bin` yang terpisah.
- `Tagihan` pada `Pengeluaran` hanya muncul saat kewajiban pembayaran masih hidup; pengeluaran direct-paid tidak dipaksa punya child bill universal.
- `Dokumen Barang` tidak melakukan stock-out otomatis; stock-out manual berjalan terbatas di route pendukung `Stok Barang` melalui boundary API resmi.
- `Payment Receipt PDF` boleh diregenerate dari data settlement resmi dan tidak pernah menggantikan record pembayaran.
- Record completed tetap muncul di `Riwayat` atau completed surface; completed tidak dihapus dari histori.

| Domain | Initial state | Active states | Completed state | Child bill needed? | Payment allowed when? | Edit allowed when? | Locked after payment? | Delete allowed when? | Restore allowed when? | Completed/history behavior | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `project income` | draft form lalu record aktif | parent aktif + child `Tagihan` `unpaid/partial/paid` | child fee `Tagihan` selesai `paid` | `Yes` | hanya pada child `Tagihan` setelah parent tersimpan | sebelum child `Tagihan` punya payment history | `Yes` | soft delete hanya bila child belum punya payment history | dari `Recycle Bin` sebagai tree parent + child | parent tetap ada di histori; settlement child tetap bisa diaudit | parent bukan surface pembayaran; payment selalu lewat child bill |
| `expense` | record baru, bisa masih payable atau langsung lunas | parent aktif; jika kewajiban masih hidup maka child `Tagihan` `unpaid/partial/paid` | `paid` langsung saat create atau child `Tagihan paid` | `Hanya jika kewajiban pembayaran masih hidup` | melalui child `Tagihan` saat child ada dan berstatus `unpaid`/`partial`; direct-paid hanya saat create commit | sebelum ada payment history atau child settlement terkunci | `Yes` | soft delete hanya bila belum ada payment history | dari `Recycle Bin` dengan tree parent yang sama | expense direct-paid maupun settled via bill tetap tampil di `Riwayat` | `Tagihan` bukan kontrak universal semua `Pengeluaran`; ia child settlement saat kewajiban masih hidup dan parent tetap `Pengeluaran` secara finansial |
| `faktur barang` | local draft | dokumen aktif; bisa menjadi dokumen finansial dan inbound-stock | `Tagihan paid` | `Yes` bila payable ada | saat child `Tagihan` `unpaid` atau `partial` | sebelum ada payment history pada child bill | `Yes` | soft delete hanya bila belum ada payment history | dari `Recycle Bin` dengan line item, attachment, dan bill tree lengkap | dokumen selesai tetap terlihat sebagai histori dokumen barang | standalone faktur boleh mencatat stok masuk; hasil konversi dari `Surat Jalan Barang` tidak boleh double count stok masuk |
| `surat jalan barang` | local draft | dokumen aktif, stok masuk sudah dicatat, belum settle finansial | `converted` ke `Faktur Barang` atau closed logistically | `No`, sampai konversi | tidak pernah langsung pada `Surat Jalan Barang`; payment baru boleh setelah konversi menjadi `Faktur Barang` | sebelum konversi dan sebelum linked settlement history ada | `Yes`, setelah linked `Faktur Barang` punya payment history atau conversion tree dikunci | soft delete hanya bila belum terikat settlement history dan reversal stok masih valid | dari `Recycle Bin` jika seluruh document tree aman dipulihkan | record converted tetap ada di histori sebagai jejak dokumen asal | stok masuk pada `Surat Jalan Barang` tidak boleh dihitung dua kali saat dikonversi |
| `loan / dana masuk` | record baru `unpaid` | `unpaid`, `partial`, `paid`, bisa membawa overdue snapshot | `paid` | `No` | saat loan `unpaid` atau `partial` | sebelum loan punya payment history | `Yes` | soft delete hanya bila belum ada payment history | dari `Recycle Bin` bila parent belum punya child settlement aktif yang melarang | loan lunas tetap muncul di histori pinjaman dan payment history | term pinjaman, bunga, tenor, dan penalti dikunci setelah payment history muncul; `partial` berarti sebagian arus kas sudah terealisasi dan sisanya outstanding |
| `salary bill / tagihan upah` | hasil generate dari child absensi `unbilled` per worker, status awal `unpaid` | `unpaid`, `partial`, `paid` | `paid` | `Yes`, bill ini sendiri adalah child payroll | saat `Tagihan Upah` `unpaid` atau `partial` | tidak diedit sebagai form bebas; koreksi hanya lewat batal rekap selama belum ada payment history | `Yes` | soft delete hanya bila belum ada payment history dan rollback payroll valid | dari `Recycle Bin` jika attendance link dan bill tree masih konsisten | `Tagihan Upah` paid tetap tampil di `Riwayat` payroll dan payment | `Tagihan Upah` adalah payroll payable hasil rekap per worker yang boleh muncul di `Jurnal` dan `Riwayat`; correction mengikuti attendance correction rule |
| `attendance record` | draft sheet dari `Halaman Absensi` / `unbilled` | `unbilled`, lalu `billed` read-only | closed lewat `Tagihan Upah paid` | `Yes`, setelah payroll bundling | tidak pernah langsung di attendance; payment hanya lewat `Tagihan Upah` | hanya saat `unbilled` | `Yes`, setelah linked `Tagihan Upah` ada; lebih keras lagi bila sudah dibayar | delete hanya saat `unbilled` | restore hanya untuk soft-deleted `unbilled` atau lewat full tree restore yang sah di `Recycle Bin` | record billed atau paid tetap bisa diaudit di histori operasional dan dikelompokkan di bawah worker | Input harian tetap lewat `Halaman Absensi`; histori/filter/rekap terjadi di `Catatan Absensi`. Jika bill belum dibayar, batal rekap dulu; jika sudah ada payment history, batalkan payment dulu |
| `payment` | draft input di workspace `Pembayaran` | active posted payment leaf | archived/void-log | parent `Tagihan` atau `Dana Masuk / Pinjaman` wajib ada | saat parent berstatus `unpaid` atau `partial` | selama payment leaf masih active dan role mengizinkan | parent settlement dan editability domain menjadi lebih ketat setelah payment tercatat | tidak boleh hard delete dari active flow; cancellation = soft delete/void-log | restore dari `Recycle Bin` bila parent masih ada | payment history selalu dipertahankan sebagai histori audit | recalc parent wajib server-side setelah create, edit, cancel, atau restore; `Payment Receipt PDF` boleh diregenerate dari data resmi ini dan bukan source of truth |
| `attachment` | file dipilih atau uploaded lalu menunggu relasi parent | attached, metadata update, archived | restored/archived history | `No` | `N/A` | saat parent masih mengizinkan dan role sesuai matrix | mengikuti lock parent; payment-proof future mengikuti parent settlement lock | soft delete relation/file sesuai parent policy; tidak boleh meninggalkan orphan | dari `Recycle Bin` jika parent dan file asset masih valid | attachment tetap bisa dilihat dari histori parent | attachment bukan alasan untuk membuka edit pada parent yang sudah terkunci |
